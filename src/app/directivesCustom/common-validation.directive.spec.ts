import { CommonValidationDirective } from './common-validation.directive';

describe('CommonValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonValidationDirective();
    expect(directive).toBeTruthy();
  });
});
