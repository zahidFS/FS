import { FilterdropdownPipe } from './filterdropdown.pipe';

describe('FilterdropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
